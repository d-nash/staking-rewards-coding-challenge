import React from "react";
import { useUsers } from "./useUsers";

interface UserSelectorProps {
  name: string;
  onChange: (e: { target: { value } }) => void;
}

export const UserSelector = () => {
  <select
};
